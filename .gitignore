# Personal photos, Polaroid scans, and exports — NEVER commit (public repo).
# Broad on purpose: no source images live at the repo root.
*.jpg
*.jpeg
*.tif
*.tiff
*_polaroid-ready.png
IMG_*
FullSizeRender*

# charts/ holds local scans + calibration artifacts. Track the generated
# sample charts and the anonymized sample calibrations; everything else
# (scans, analysis images) stays local regardless of name/case.
charts/*
!charts/calibration-chart-*.png
!charts/calibration-color.json
!charts/calibration-bw.json

# Tooling / OS / local config
node_modules/
.DS_Store
.claude/settings.local.json
