# Personal photos, Polaroid scans, and exports — NEVER commit (public repo).
# Broad on purpose: no source images live at the repo root.
*.jpg
*.jpeg
*.tif
*.tiff
*_polaroid-ready.png
IMG_*
FullSizeRender*

# charts/ holds local scans + calibration artifacts. Track ONLY the generated
# sample charts; everything else stays local regardless of name/case.
charts/*
!charts/calibration-chart-*.png

# Tooling / OS / local config
node_modules/
.DS_Store
.claude/settings.local.json
